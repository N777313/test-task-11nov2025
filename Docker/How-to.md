cat How-to.md
Отлично 🎯 — по логам теперь всё выглядит **идеально**!
Контейнер PostgreSQL 12.10 **инициализировался с нуля**, ошибок нет, и сервер уже слушает на `0.0.0.0:5432`.

---

Теперь нужно просто подключиться **под тем пользователем, которого ты указал в docker-compose** 👇

📌 У тебя в `docker-compose.yml`:

```yaml
POSTGRES_USER: admin
POSTGRES_PASSWORD: admin123
POSTGRES_DB: testdb
```

---

### 🔹 Проверь подключение:

```bash
docker exec -it postgres12 psql -U admin -d testdb -c "select version();"
```

Если всё сделано правильно — ты должен увидеть:

```
                                                 version
-----------------------------------------------------------------------------------------------------------
 PostgreSQL 12.10 (Debian 12.10-1.pgdg110+1) on x86_64-pc-linux-gnu, compiled by gcc (Debian 10.2.1-6), 64-bit
(1 row)
```

---

Если хочешь убедиться, что действительно есть этот пользователь и база:

```bash
docker exec -it postgres12 psql -U admin -d testdb
```

и внутри psql выполни:

```sql
\du
\l
```

---

После этого можно будет:
✅ Подключаться с другого компьютера в локальной сети
✅ Настроить `pg_hba.conf` для доверенного доступа
✅ Или использовать pgAdmin / DBeaver, указав:

```
Host: <IP твоей машины>
Port: 5432
Database: testdb
User: admin
Password: admin123
```

